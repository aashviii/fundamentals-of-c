#include<stdio.h>
int main()
{
	int c,area;
	printf("enter the area of the rectangle");
	scanf("%d",&area),
	c = 4 * area;
	printf("the circumference of the rectangle is %d:",c,area);
	return 0;
}
