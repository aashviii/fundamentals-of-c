#include<stdio.h>
int main()
{
	int l,b,area;
	printf("enter the length and the width:");
	scanf("%d %d",&l,&b);
	area=l*b;
	printf("length is %d and width is %d \nso area is %d",l,b,area);
	return 0;
}
