#include<stdio.h>
int main()
{
	float area,b,h;
	printf("enter the base and height of triangle: ");
	scanf("%f %f",&b,&h);
	area=(b*h) / 2;
	printf("the area of triangle is: %2f",area);
	printf("the area of triangle is: %2f",area);
	return 0;
	}
